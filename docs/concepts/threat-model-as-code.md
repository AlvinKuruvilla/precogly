# Threat Model as Code

Precogly lets you export any threat model as a structured JSON file so you can store it in version control, diff changes over time, and integrate threat modeling into your development workflow.

!!! note "Coming soon"
    This page will cover:

    - Exporting and importing threat models as JSON
    - The TM-Library format (components, threats, controls, risks, data flows)
    - Round-trip fidelity between Precogly and JSON
    - Version control workflows (git diff, pull request reviews)
    - Interoperability with other threat modeling tools
